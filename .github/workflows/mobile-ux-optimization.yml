name: Mobile UX Regression

on:
  push:
    branches:
      - fix/mobile-ux-optimization-20260903

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run mobile UX regression test
        run: python3 tests/mobile_ux_regression.py
