name: SKETCHUP Project Regression
on:
  push:
    branches:
      - main
      - feature/sketchup-project
  pull_request:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 tests/sketchup_project_regression.py
